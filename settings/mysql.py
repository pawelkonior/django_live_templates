DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.mysql",
        "NAME": "$db_name$",
        "USER": "$db_username$",
        "PASSWORD": "$password$",
        "HOST": "$db_host$",
        "PORT": 3306,
    }
}
