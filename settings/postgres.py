DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": "$db_name$",
        "USER": "$db_username$",
        "PASSWORD": "$password$",
        "HOST": "$db_host$",
        "PORT": 5432,
    }
}
